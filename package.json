{
  "name": "website",
  "version": "0.1.0",
  "private": true,
  "homepage": "http://iuga.info/",
  "devDependencies": {
    "react-scripts": "0.9.0",
    "rsync": "^0.6.1"
  },
  "dependencies": {
    "axios": "^0.15.3",
    "classnames": "^2.2.5",
    "react": "^15.4.2",
    "react-dom": "^15.4.2",
    "react-router": "^3.0.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "npm run build; node ./scripts/deploy.js",
    "deploy-beta": "npm run build; node ./scripts/deploy.js --path=beta/"
  }
}
